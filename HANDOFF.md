# 자리왔나 인수인계 보고서

최종 갱신: 2026-09-19

## 1. 프로젝트 목적

`자리왔나`는 버스와 KTX의 가상 빈자리 알림 흐름을 체험하는 수업용 모바일 목업이다. 실제 좌석 조회, 알림 발송, 예약, 로그인, DB, 외부 API는 구현 범위에 포함하지 않는다.

핵심 흐름은 다음과 같다.

```text
교통수단 선택 → 매진 시간 선택 → 알림 설정 → 빈자리 예시 확인 → 예약 불가 안내
```

## 2. 현재 구현 상태

| 영역 | 완료 상태 | 구현 내용 |
| --- | --- | --- |
| 첫 화면 | 완료 | 고속버스/KTX 탭, 서울→대전 조건, 시간 카드 3개, 선택 불가 상태 안내 |
| 알림 대기 | 완료 | 선택한 교통수단·시간·노선·좌석 상태를 유지해 표시 |
| 결과 | 완료 | 선택 시간별 가상 빈자리 수와 확인 시각 표시 |
| 예약 안내 | 완료 | 실제 예약 대신 시연용 모달 안내 표시 |
| 반응형 | 완료 | 390px 모바일 단일 열 기준으로 검토 |
| 정적 빌드 | 완료 | `next build`로 `out/` 정적 결과 생성 |

현재 UI는 [app/page.tsx](app/page.tsx)에, 스타일은 [app/globals.css](app/globals.css)에 있다. 가상 운행 데이터와 화면 상태도 `app/page.tsx`에서 관리한다.

## 3. 가상 데이터 규칙

| 교통수단 | 매진 선택 가능 | 잔여석 표시만 | 결과 예시 |
| --- | --- | --- | --- |
| 고속버스 | 14:20, 16:00 | 15:10 (1석 남음) | 14:20→1자리·14:02 / 16:00→2자리·15:42 |
| KTX | 14:30, 16:10 | 15:20 (1석 남음) | 14:30→1자리·14:12 / 16:10→2자리·15:52 |

교통수단을 전환하면 기존 시간 선택은 초기화된다. 결과 화면의 모든 내용은 선택된 운행 데이터에서 읽어야 한다.

## 4. 확인한 항목

- 버스 16:00 선택 후 대기 화면과 `2자리` 결과 화면에 선택 정보가 유지됨.
- KTX 14:30 선택 후 `1자리` 결과 화면에 선택 정보가 유지됨.
- 잔여 1석 카드는 알림 대상으로 선택되지 않고 안내 문구를 표시함.
- 예약 화면 보기 버튼은 실제 예약 대신 시연용 모달을 표시함.
- 390px 폭에서 가로 넘침 없이 하단 행동 버튼과 공통 고지가 노출됨.
- `npm run typecheck` 및 `npm run build` 통과.

## 5. 다음 AI 작업 지시

1. 이 문서와 [PRD.md](PRD.md), [README.md](README.md)를 먼저 읽고 현재 범위를 유지한다.
2. 요청한 변경이 있을 때만 구현한다. 실제 API, 로그인, DB, 푸시 알림, 결제·예약 기능은 사용자가 명시적으로 요청하기 전까지 추가하지 않는다.
3. 수정 전에는 관련 파일을 읽고, 기존의 버스·KTX 선택과 세 단계 흐름이 깨지지 않도록 한다.
4. UI나 흐름을 바꿨다면 `npm run typecheck`과 `npm run build`를 실행한다. 상호작용을 바꿨다면 브라우저에서 대표 흐름도 확인한다.
5. 완료한 작업은 아래 작업 기록에 한 항목으로 추가한다. 날짜, 변경 파일, 사용자 관점의 변화, 검증 결과를 간단히 남긴다.
6. 이 문서는 인수인계의 기준 문서다. 구현 상태나 남은 일이 달라지면 해당 내용을 먼저 갱신한다.

## 6. 다음 진행 후보

현재 필수 작업은 없다. 사용자의 다음 요청에 따라 아래 중 필요한 항목만 진행한다.

- 문구·색상·간격 등 UI 보완
- 시간표 또는 가상 노선 데이터 확장
- Vercel 배포 준비 및 배포
- 실제 서비스 전환을 위한 별도 PRD 작성

## 7. 작업 기록

| 날짜 | 담당 | 변경 내용 | 검증 |
| --- | --- | --- | --- |
| 2026-09-19 | Codex | Next.js·TypeScript 기반 버스/KTX 빈자리 알림 목업 구현. 3단계 체험 흐름, 가상 결과, 예약 안내 모달, 390px 모바일 UI 추가. | `npm run typecheck`, `npm run build`, 대표 클릭 흐름, 390px 화면 확인 |
| 2026-09-19 | Antigravity | 전체 디자인을 청록색에서 파란색(#1a6fd4) 계열로 변경(globals.css). 예약 모달을 실제 예약 사이트 링크 카드로 개선: KTX → 코레일(letskorail.com·korail.com), 고속버스 → 코버스(kobus.co.kr)·이지티켓(easytis.com). 선택 교통수단에 따라 모달 링크 자동 전환. | `tsc --noEmit` 통과, `next build` 정적 빌드 통과 |
| 2026-09-19 | Antigravity | **실 API 연동** — 가상 데이터 제거. 공공데이터포털 코레일 열차운행정보 API(`/B551457/run/v2`)·TAGO 고속버스정보 API(`/1613000/ExpBusInfo`) 연동. API 키는 `.env.local`에서 서버에서만 사용. `app/api/trains/route.ts`, `app/api/buses/route.ts` 추가. 출발·도착·날짜 직접 선택, 실제 시간표 조회, 요금 표시, 코레일·코버스 공식 예약 사이트 연결. | `tsc --noEmit` 통과 |

