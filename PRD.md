# 자리왔나 — 버스 빈자리 알림 체험 목업 PRD

## 1. 목표와 범위

**한 줄 정의:** 원하는 버스 시간대의 빈자리가 생기는 상황을 빠르게 확인하고 다음 행동을 이해하는, 수업용 모바일 목업이다.

학생 또는 가족이 서울에서 대전으로 가는 버스 중 매진된 시간대를 고른 뒤, 준비된 빈자리 발생 예시를 확인한다. 실제 버스 조회, 알림 발송, 예약은 수행하지 않는다.

### 이번 목업에 포함

- 서울 → 대전, 9월 26일, 우등 버스라는 가상 조건
- 매진 시간 선택, 알림 설정, 빈자리 예시 재생
- 선택 시간별로 달라지는 예시 좌석 수와 확인 시각
- 예약 버튼을 눌렀을 때의 시연용 안내

### 이번 목업에 포함하지 않음

- 실제 버스 좌석 조회·예약 연동
- 문자, 푸시 등 실제 알림 발송
- 로그인, 회원가입, DB 저장, 외부 API

## 2. 사용자와 핵심 흐름

**대상 사용자:** 특정 시간대 버스의 빈자리를 기다리는 학생 또는 가족.

1. 사용자가 서울 → 대전, 9월 26일 조건과 시간대 목록을 확인한다.
2. 매진된 시간(14:20 또는 16:00)을 선택한다.
3. **이 시간 빈자리 알림 설정**을 눌러 알림 대기 화면으로 이동한다.
4. **빈자리 발생 예시 보기**를 눌러 선택 시간에 연결된 결과를 확인한다.
5. **예약 화면 보기**를 누르면 실제 예약 대신 시연용 안내를 확인한다.

완료 목표는 이 흐름을 클릭만으로 60~90초 안에 체험하는 것이다.

## 3. 화면 구성

### 화면 1 — 버스 선택

- 상단 제목: **어느 버스를 기다리시나요?**
- 검색 조건: 출발 `서울`, 도착 `대전`, 날짜 `9월 26일`, 등급 `우등`
- 가로 타임라인으로 서울 → 대전과 선택 시간을 연결해 표시
- 시간대 카드
  - `14:20` / `매진` / 알림 선택 가능
  - `15:10` / `1석 남음` / 알림 대상 아님
  - `16:00` / `매진` / 알림 선택 가능
- 매진 카드를 선택하면 청록색 테두리와 선택 상태가 적용된다.
- 하단 고정 주요 버튼: **이 시간 빈자리 알림 설정**
  - 매진 시간이 선택됐을 때만 활성화한다.
  - 선택 전에는 비활성 상태이며, 누르려고 하면 `기다릴 시간을 먼저 선택해 주세요`를 표시한다.
- 15:10 카드 선택 시에는 선택을 저장하지 않고 `현재 1석 남은 시간은 빈자리 알림 대상이 아니에요`를 표시한다.

### 화면 2 — 알림 대기

- 제목: **{선택 시간} 버스를 기다리고 있어요**
- 큰 조건 카드: `서울 → 대전`, `9월 26일`, `우등`, `{선택 시간}`
- 상태 배지: **현재 매진**
- 안내 문구: `목업에서는 실제 좌석을 조회하지 않습니다`
- 주요 버튼: **빈자리 발생 예시 보기**
- 보조 버튼: **시간 다시 선택** — 화면 1로 돌아가 선택을 바꾼다.

### 화면 3 — 빈자리 결과

- 강조 카드
  - 제목: **{선택 시간} 버스에 {좌석 수}자리가 생겼어요**
  - 보조 정보: `예시 확인 시각 {확인 시각}`
  - `준비된 빈자리 변화 예시입니다`를 명시한다.
- 선택 조건 요약: 서울 → 대전 / 9월 26일 / 우등 / 선택 시간
- 주요 버튼: **예약 화면 보기**
  - 누르면 토스트 또는 인라인 안내: `시연용 화면이라 예약은 진행되지 않아요`
- 보조 동작: 시간 다시 선택으로 돌아갈 수 있다.

## 4. 샘플 데이터 및 상태

모든 데이터는 완전한 가상 시연 데이터다.

```ts
type BusOption = {
  departureTime: '14:20' | '15:10' | '16:00';
  status: 'sold-out' | 'available';
  seats?: number;
  alertEligible: boolean;
  example?: {
    checkedAt: string;
    seats: number;
  };
};

const busOptions: BusOption[] = [
  {
    departureTime: '14:20',
    status: 'sold-out',
    alertEligible: true,
    example: { checkedAt: '14:02', seats: 1 },
  },
  {
    departureTime: '15:10',
    status: 'available',
    seats: 1,
    alertEligible: false,
  },
  {
    departureTime: '16:00',
    status: 'sold-out',
    alertEligible: true,
    example: { checkedAt: '15:42', seats: 2 },
  },
];
```

최소 React 상태는 다음으로 구성한다.

- `selectedTime`: 선택된 매진 시간 또는 `null`
- `screen`: `'select' | 'waiting' | 'result'`
- `showReservationNotice`: 예약 안내 노출 여부
- `notice`: 시간 미선택 또는 15:10 선택 안내 메시지

선택한 시간의 `example`을 결과 화면에 그대로 사용한다. 따라서 14:20은 14:02의 1자리, 16:00은 15:42의 2자리 결과를 보여준다.

## 5. 상호작용 규칙

| 행동 | 결과 |
| --- | --- |
| 14:20 또는 16:00 카드 선택 | 선택 카드 테두리를 청록색으로 바꾸고 설정 버튼을 활성화 |
| 15:10 카드 선택 | 선택 상태를 만들지 않고 알림 대상 아님 안내 표시 |
| 시간 미선택 상태의 설정 시도 | 설정 화면으로 이동하지 않고 시간 선택 안내 표시 |
| 알림 설정 | 선택 시간이 유지된 알림 대기 화면으로 이동 |
| 빈자리 발생 예시 보기 | 선택 시간의 예시 데이터로 결과 화면 전환 |
| 예약 화면 보기 | 실제 예약 없이 시연용 안내 표시 |
| 시간 다시 선택 | 선택 화면으로 돌아가 다른 매진 시간을 고를 수 있음 |

## 6. 시각 및 접근성 기준

- 기준 화면 폭은 **390px**이며, 모바일 단일 열로 구성한다.
- 위계는 `상단 질문 → 검색 조건 → 시간 카드 → 고정 하단 버튼` 순서다.
- 배경은 옅은 회색, 기본 글자는 짙은 남색을 사용한다.
- 선택 상태와 주요 버튼에는 특정 서비스 디자인을 복제하지 않은 선명한 청록색을 사용한다.
- 제목은 24px 굵게, 결과의 핵심 좌석 수는 32px, 본문은 15~16px로 설정한다.
- 색만으로 상태를 구분하지 않고 `매진`, `1석 남음`, `2자리 가능` 텍스트를 함께 표시한다.
- 사진은 사용하지 않는다.

## 7. 공통 고지

모든 화면 하단에 다음 고지를 항상 표시한다.

> 수업용 목업입니다. 노선·시간·좌석은 모두 가상 데이터이며 실제 조회, 알림 발송, 예약은 지원하지 않습니다.

## 8. 완료 기준

- 매진 시간 선택 → 알림 설정 → 빈자리 예시 확인을 60~90초 안에 완료할 수 있다.
- 선택한 시간과 노선이 대기·결과 화면에서도 일관되게 유지된다.
- 14:20과 16:00은 각자 지정된 빈자리 예시로 연결된다.
- Next.js, TypeScript, 샘플 배열, React 상태만으로 동작한다.
- 로그인, DB, 외부 API를 요구하지 않는다.

## 구현 확장 — 버스 + KTX

- 교통수단 탭: 고속버스 / KTX. 변경하면 시간 선택을 초기화한다.
- 공통 날짜: 2026년 9월 26일. 버스 서울 → 대전, KTX 서울역 → 대전역.
- KTX 가상 데이터: 14:30 출발(매진 → 1자리, 확인 14:12), 15:20 출발(1석 남음, 선택 불가), 16:10 출발(매진 → 2자리, 확인 15:52).
- 버스 원본 예시: 14:20(1자리, 확인 14:02), 15:10(선택 불가), 16:00(2자리, 확인 15:42).
- 교통수단·시간 선택 → 알림 대기 → 빈자리 결과의 3개 화면을 React 상태로 전환한다.
- 예약 화면 보기 버튼은 예약 대신 모달 안내를 표시한다. 새로고침 시 상태는 초기화된다.
- Next.js + TypeScript, 외부 API·로그인·DB·실제 알림 발송 없음. Vercel 배포는 선택 사항이다.
